async function getBooks(
  page: number,
  items: number,
  search?: string,
  sortColumn?: symbol,
  sortKey?: keyof Book,
  order: SortDirection = 'asc',
) {
  // function body
} satisfies DataGridSource<Book>;
